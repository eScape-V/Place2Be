{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Mon profil
{% endblock %}

{% block body %}
    <h2>Mon profil</h2>
    <div>
        <div>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button>Enregistrer</button>
            <button type="reset">Annuler</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

