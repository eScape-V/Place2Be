<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('main_home') }}"><img src="{{ asset('images/Logo_transparent.png') }}" alt="Place2Be"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('main_home') }}">Accueil
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('main_home') }}">Villes
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('main_home') }}">Campus
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                {% endif %}
                {% if not(app.user) %}
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_register') }}">Créer un compte
                            <span class="visually-hidden">(current)</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_login') }}">Se connecter
                            <span class="visually-hidden">(current)</span>
                        </a>
                    </li>
                {% else %}
                    <!--<li class="nav-item">
                        <a class="nav-link active" href="{# {{ path('modifierProfil') }}#}">Mon profil
                            <span class="visually-hidden">(current)</span>
                        </a>
                    </li>-->
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_logout') }}">Se déconnecter
                            <span class="visually-hidden">(current)</span>
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>