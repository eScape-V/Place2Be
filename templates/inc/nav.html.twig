

<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('main_home') }}"><img src="{{ asset('images/Logo_transparent.png') }}" alt="Place2Be"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('main_home') }}">Accueil
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <!--{#{% if not(app.user) %}
                    <li><a href="{{ path('app_register') }}" title="Inscription">Inscription</a></li>
                    <li><a href="{{ path('app_login') }}" title="Connexion">Connexion</a></li>
                {% else %}
                    <li><a href="{{ path('app_logout') }}" title="Déconnexion">Déconnexion ({{ app.user.email|split('@')[0]}})</a></li>
                {% endif %}#}-->
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('main_home') }}">Villes
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('main_home') }}">Campus
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('main_home') }}">Mon profil
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('main_home') }}">Se déconnecter
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>